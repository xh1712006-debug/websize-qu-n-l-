<div class="space-y-10 animate-in fade-in slide-in-from-bottom-6 duration-700 pb-20 max-w-5xl mx-auto">

    <!-- PAGE HEADER -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
        <div class="flex items-center gap-5">
            <div class="w-16 h-16 bg-blue-600 rounded-3xl flex items-center justify-center text-3xl shadow-xl shadow-blue-100">
                👨‍🎓
            </div>
            <div>
                <h1 class="text-3xl font-black text-slate-900 tracking-tight">Danh sách Đăng ký</h1>
                <p class="text-slate-500 font-medium mt-1">Quản lý và phê duyệt sinh viên tham gia đồ án tốt nghiệp</p>
            </div>
        </div>
        
        <div id="countStudent" class="px-6 py-3 bg-white border border-slate-200 rounded-2xl text-[10px] font-black text-blue-600 uppercase tracking-widest shadow-sm">
            0 sinh viên
        </div>
    </div>

    <!-- MAIN CARD -->
    <div class="bg-white rounded-[3.5rem] shadow-sm border border-slate-100 overflow-hidden relative">
        <div class="absolute top-0 right-0 w-80 h-80 bg-slate-50 rounded-full -mr-40 -mt-40 opacity-40"></div>
        
        <div class="p-10 md:p-14 space-y-10 relative z-10">
            <!-- SEARCH -->
            <div class="relative group">
                <input id="searchInput" type="text" placeholder="Tìm kiếm tên hoặc mã số sinh viên..."
                    class="w-full bg-slate-50 border-2 border-transparent focus:border-blue-500 focus:bg-white rounded-2xl p-5 pl-14 text-sm font-bold text-slate-700 outline-none transition-all shadow-inner">
                <div class="absolute left-6 top-1/2 -translate-y-1/2 text-slate-400">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                </div>
            </div>

            <!-- LIST CONTAINER -->
            <div id="studentList" class="grid grid-cols-1 md:grid-cols-2 gap-6 max-h-[600px] overflow-y-auto pr-2 custom-scrollbar p-1">
                <!-- Dynamic cards will be injected by viewStudent.js -->
            </div>

            <!-- EMPTY STATE -->
            <div id="empty" class="hidden py-24 text-center">
                <div class="w-20 h-20 bg-slate-50 rounded-full flex items-center justify-center mx-auto mb-6 text-4xl opacity-50">🏜️</div>
                <h3 class="text-2xl font-black text-slate-900">Danh sách trống</h3>
                <p class="text-slate-500 font-medium mt-2">Hiện tại chưa có sinh viên nào đăng ký đề tài này.</p>
            </div>
        </div>
    </div>

    <!-- BACK BUTTON -->
    <div class="flex justify-center">
        <a href="/admin/project" class="flex items-center gap-2 text-[10px] font-black text-slate-400 uppercase tracking-widest hover:text-blue-600 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            Quay lại kho đồ án
        </a>
    </div>

</div>

<script src="/js/admin/project/viewStudent.js"></script>
