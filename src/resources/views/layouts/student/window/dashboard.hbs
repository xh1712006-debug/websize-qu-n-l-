<div class="space-y-10">
{{#each data}}

  <!-- HEADER -->
  <div class="flex justify-between items-center">
    <div>
      <h1 class="text-3xl font-bold text-gray-800">📊 Dashboard</h1>
      <p class="text-sm text-gray-500 mt-1">
        Tổng quan tiến độ đồ án tốt nghiệp của bạn
      </p>
    </div>

    <span class="px-4 py-2 bg-blue-100 text-blue-700 rounded-full text-sm">
      Năm học 2025 – 2026
    </span>
  </div>

  <!-- QUICK STATS -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">

    <div class="bg-white p-6 rounded-2xl shadow border-l-4 border-blue-500">
      <p class="text-sm text-gray-500">Đồ án</p>
      <p class="text-4xl font-bold text-blue-600 mt-2">{{projectCount}}</p>
      <p class="text-xs text-gray-400 mt-1">Đang thực hiện</p>
    </div>

    <div class="bg-white p-6 rounded-2xl shadow border-l-4 border-green-500">
      <p class="text-sm text-gray-500">Tiến độ</p>
      <p class="text-4xl font-bold text-green-600 mt-2">{{precent}}%</p>

      <div class="w-full bg-gray-200 rounded-full h-2 mt-3">
        <div class="bg-green-500 h-2 rounded-full" style="width: {{precent}}%"></div>
      </div>
    </div>

    <div class="bg-white p-6 rounded-2xl shadow border-l-4 border-yellow-500">
      <p class="text-sm text-gray-500">Báo cáo đã nộp</p>
      <p class="text-4xl font-bold text-yellow-600 mt-2">{{report.length}}</p>
      <p class="text-xs text-gray-400 mt-1">Tuần / Giai đoạn</p>
    </div>

    <div class="bg-white p-6 rounded-2xl shadow border-l-4 border-purple-500">
      <p class="text-sm text-gray-500">Phản hồi GV</p>
      <p class="text-4xl font-bold text-purple-600 mt-2">{{feedbackCount}}</p>
      <p class="text-xs text-gray-400 mt-1">Chưa đọc</p>
    </div>

  </div>

  <!-- TODO + FEEDBACK -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">

    <!-- TODO -->
    <div class="bg-white p-6 rounded-2xl shadow">
      <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
        📌 Việc cần làm
      </h2>

      <ul class="space-y-4 text-sm">

        <li class="flex justify-between items-center">
          <div>
            <p class="font-medium">Nộp báo cáo tuần {{week}}</p>
            <p class="text-xs text-gray-500">Đề tài: {{projectName}}</p>
          </div>
          <span class="px-3 py-1 bg-red-100 text-red-600 rounded-full">
            Hôm nay
          </span>
        </li>

        <li class="flex justify-between items-center">
          <div>
            <p class="font-medium">Chỉnh sửa đề cương</p>
            <p class="text-xs text-gray-500">Theo góp ý GVHD</p>
          </div>
          <span class="px-3 py-1 bg-yellow-100 text-yellow-600 rounded-full">
            Còn {{day}} ngày
          </span>
        </li>

      </ul>
    </div>

    <!-- NEW FEEDBACK -->
    <div class="bg-white p-6 rounded-2xl shadow">
      <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
        💬 Phản hồi mới từ giảng viên
      </h2>

      <div class="border-l-4 border-yellow-500 pl-4">
        <p class="text-sm text-gray-700 italic">
          “{{feedbackContent}}”
        </p>

        <p class="text-xs text-gray-400 mt-2">
          {{teacherName}} · {{FeedbackDate}}
        </p>
      </div>

      <div class="mt-4 text-right">
        <a href="/student/feedback"
           class="text-blue-600 text-sm font-medium hover:underline">
          Xem chi tiết →
        </a>
      </div>
    </div>

  </div>

  <!-- PROJECT INFO -->
  <div class="bg-white p-6 rounded-2xl shadow">
    <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
      📚 Đồ án đang thực hiện
    </h2>

    <table class="w-full text-sm">
      <thead>
        <tr class="border-b text-gray-500">
          <th class="py-2 text-left">Tên đồ án</th>
          <th class="text-left">GVHD</th>
          <th class="text-left">Tiến độ</th>
          <th class="text-left">Trạng thái</th>
        </tr>
      </thead>

      <tbody>
        <tr class="border-b">
          <td class="py-3 font-medium">
            {{projectName}}
          </td>
          <td>{{teacherName}}</td>
          <td>{{precent}}%</td>
          <td>
            <span class="px-3 py-1 bg-green-100 text-green-700 rounded-full text-xs">
              {{projectStatus}}
            </span>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
{{/each}}

</div>

